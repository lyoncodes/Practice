'use strict'
const {
 GAME_SPEED,
 DIRECTIONS,
 INITIAL_SNAKE_SIZE,
 SNAKE_COLOR,
 DOT_COLOR,
} = require('./constants.js')

class Game {
 constructor(ui) {
  this.ui = ui

  //Bind handlers to UI so we can detect input change from the Game class

 }
}
module.exports = { Game };